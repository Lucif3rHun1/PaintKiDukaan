# Debug Journal — Settings navigation and form state
Started: 2026-07-18
Goal: Fix stale Settings sidebar highlights and Shop settings dirty, status, validation, and save behavior.

## Environment snapshot
- Runtime: React 19 + TypeScript 5.7 + Vite 5.4, launched by pnpm/Vite inside Tauri
- Entry: `pnpm dev` / `pnpm tauri:dev`, ports 1420 and 1421
- References read: TypeScript, frontend design/perfection, Node debugging, Playwright QA, debug setup/fix/QA/cleanup

## Hypotheses
1. [CONFIRMED BY SOURCE] AppShell does not subscribe to hash changes, so same-tab Settings navigation cannot refresh active-link rendering.
2. [CONFIRMED BY SOURCE] Settings active-link matching only recognizes category-prefixed routes, so legacy bare item hashes have no active category.
3. [CONFIRMED BY SOURCE] Shop forms do not retain loaded baselines or register dirty state; concurrent writes and missing validation expose partial-save and invalid-input states.

## Artifacts to revert
- [ ] `src/shell/AppShell.test.tsx` — regression tests; keep as part of fix.
- [ ] `src/shell/routes/settings/ShopSettings.test.tsx` — regression tests; keep as part of fix.
- [ ] `src/shell/AppShell.tsx` — production fix; keep as part of fix.
- [ ] `src/shell/settingsCategories.ts` — legacy route categorization; keep as part of fix.
- [ ] `src/shell/routes/settings/ShopSettings.tsx` — production fix; keep as part of fix.
- [ ] Any browser screenshots/traces under `/tmp`; remove after QA.

## Findings

## Final fix
